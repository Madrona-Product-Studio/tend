VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
# Waitlist email capture on the landing page (Formspree-compatible JSON POST
# endpoint). The form only renders when this is set.
VITE_WAITLIST_ENDPOINT=
